<%= link_to "Teams", teams_path(@team)%>


<ul>
<h1> All Players</h1>
  <% @players.each do |player| %>
    <li>
      <%= link_to player.name, player_path(player) %>
    </li>
    <br>
  <% end %>

</ul>